{
	"scripts": {
		"start": "razzle build && cross-env NODE_ENV=production node build/server.js"
		, "dev": "razzle start"
	}
	, "dependencies": {
		"express": "4.x"
		, "compression": "1.x"
		, "html-minifier": "3.x"

		, "react": "16.x"
		, "react-dom": "16.x"
		, "styled-components": "2.x"

		, "material-ui": "0.x"
	}
	, "devDependencies": {
		"cross-env": "5.x"
		, "razzle": "0.8.x"
	}
}